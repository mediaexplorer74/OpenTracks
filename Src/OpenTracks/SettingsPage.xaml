<Page
    x:Class="OpenTracksBETA.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenTracksBETA"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <!-- Background Image -->
        <Image Source="Assets/bg1.png" Stretch="UniformToFill" />

        <!-- Pivot control instead of Hub -->
        <Pivot Title="Settings" Background="Transparent">

            <!-- GENERAL SETTINGS -->
            <PivotItem Header="General">
                <StackPanel Margin="20">
                    <TextBlock Text="Enable Notifications:" FontSize="24" Margin="0,0,0,5"/>
                    <Button x:Name="NotificationsButton" Content="Notifications: ON" Click="NotificationsButton_Click" Width="300" Height="70" />

                    <TextBlock Text="Auto Sync:" FontSize="24" Margin="20,0,0,5"/>
                    <Button x:Name="SyncButton" Content="Auto Sync: OFF" Click="SyncButton_Click" Width="300" Height="70" />
                </StackPanel>
            </PivotItem>

            <!-- AUDIO SETTINGS -->
            <PivotItem Header="Audio">
                <StackPanel Margin="20">
                    <TextBlock Text="Equalizer Preset:" FontSize="24" Margin="0,0,0,5"/>
                    <ListView x:Name="EqualizerListView" Height="150">
                        <ListViewItem Content="Pop"/>
                        <ListViewItem Content="Rock"/>
                        <ListViewItem Content="Jazz"/>
                        <ListViewItem Content="Custom"/>
                    </ListView>

                    <TextBlock Text="Volume Boost:" FontSize="24" Margin="20,0,0,5"/>
                    <Slider Minimum="0" Maximum="100" Value="50"/>
                </StackPanel>
            </PivotItem>

            <!-- THEME SETTINGS -->
            <PivotItem Header="Theme">
                <StackPanel Margin="20">
                    <TextBlock Text="App Theme:" FontSize="24" Margin="0,0,0,5"/>

                    <RadioButton Content="Light"
                     GroupName="Theme"
                     Checked="ThemeRadio_Checked"
                     Tag="Light"
                     IsChecked="False"/>

                    <RadioButton Content="Dark"
                     GroupName="Theme"
                     Checked="ThemeRadio_Checked"
                     Tag="Dark"
                     IsChecked="False"/>

                    <RadioButton Content="Gradient"
                     GroupName="Theme"
                     Checked="ThemeRadio_Checked"
                     Tag="Gradient"
                     IsChecked="False"/>
                </StackPanel>
            </PivotItem>

        </Pivot>
    </Grid>
</Page>
